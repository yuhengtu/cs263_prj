# Core scientific computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0
statsmodels>=0.14.0

# IRT and reliability tooling
py-irt
pyrelimri
pyro-ppl

# Deep learning (PyTorch 2.4+ recommended for Python 3.10)
# Note: For Python 3.10, use torch==2.4.0
# For Python 3.11+, use torch>=2.5.0
torch>=2.4.0
torchmetrics>=1.0.0

# Transformers and LLM libraries
transformers>=4.30.0
datasets>=2.10.0
trl>=0.12.1

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
tueplots>=0.2.0

# Utilities
tqdm>=4.65.0
joblib>=1.3.0
python-dotenv>=1.0.0
wandb>=0.15.0
fastparquet>=2023.0.0
pyarrow>=12.0.0
huggingface_hub>=0.16.0

# Optional: For web scraping (only needed for data collection)
selenium>=4.10.0
webdriver_manager>=3.8.0

# Optional: Inference acceleration (requires CUDA and Python 3.10)
# Uncomment if needed and you have CUDA installed:
# flash-attn>=2.5.0 --no-build-isolation
# vllm>=0.6.3

# Optional: Additional utilities
# boardlaw - uncomment if needed:
# git+https://github.com/andyljones/boardlaw.git#egg=boardlaw

# Optional: AI2/OLMo integration (only needed for DataDecide pipeline)
# ai2-olmo
# git+https://github.com/allenai/OLMo-ladder@datados#egg=ladder[plotting]

# Note: If using Python 3.11+, some packages may need updated versions
# If flash-attn installation fails, it's optional for basic experiments
